<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.prodadimhaski.lastwill">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
        <activity android:name=".ui.mainActivities.NewWillActivity"></activity>
        <activity android:name=".ui.startActivities.RecoveryPasswordActivity" />
        <activity android:name=".ui.mainActivities.UserActivity" />
        <activity android:name=".ui.startActivities.MainActivity" />
        <activity android:name=".ui.startActivities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>